Refreshing the base search URL https://www.gtlaw.com.au/search produces an inconsistent search state. 

On refresh/re-enter, the same base search results page alternates between displaying the expected default All results state and an incorrect 0 results.

The base search URL should consistently load the default search landing state with the expected results rather than intermittently displaying an empty-results state.