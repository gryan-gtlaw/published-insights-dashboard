# Dashboard data

Upload the current publication export to this folder using the permanent filename:

`published-insights.csv`

The dashboard requests that file automatically whenever the page opens. Replacing the file in GitHub and committing the change updates the permanent dashboard data without rebuilding the application.

Keep the expected source column headings unchanged where possible. If headings change, use the dashboard's column-mapping controls to review the inferred mappings.
